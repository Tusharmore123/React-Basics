import React from "react";
const UserContext=React.createContext();
export default UserContext;


// create a context
// the context is generally used to provide the values globally
// the provider is used to wrap the component for which the context should be provided
